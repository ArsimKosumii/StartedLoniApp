import { StatusBar } from "expo-status-bar";
import { Text, View } from "react-native";

export default function Header() {
  return <StatusBar style="dark" backgroundColor="transparent" translucent />;
}
